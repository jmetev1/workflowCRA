import React, { Fragment, useState } from 'react';

const Home = ({ user }) => (
  <Fragment>
    home
  </Fragment>
)


export default Home;
//    background-image: linear-gradient(to bottom, #0788DE, #116AB8);